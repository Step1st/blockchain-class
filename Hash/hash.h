#include <string>
#include <bitset>
#include <vector>
#include <random>
#include <unordered_map>
#include <algorithm>


std::string bin_to_hex(std::string binary);
std::string hash(std::string input, bool bin = false);